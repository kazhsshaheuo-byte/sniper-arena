-- Sniper Arena Script by wizzey (PREMIUM ANIMATED + GFX SHADERS 2026)
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local Lighting = game:GetService("Lighting") -- Сервис освещения
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

local ScriptActive = true

local TopGui = nil
if gethui then
    TopGui = gethui()
elseif game:GetService("CoreGui"):FindFirstChild("RobloxGui") then
    TopGui = game:GetService("CoreGui")
else
    TopGui = LocalPlayer:WaitForChild("PlayerGui")
end

-- ===== НАСТРОЙКИ =====
local Settings = {
    Aimbot = true,
    AimbotFOV = 250, 
    AimbotKey = Enum.KeyCode.E,
    TriggerBot = true,
    ESP_Box = true,
    ShowFOV = true,
    ExpandHeads = false,
    HeadSize = 6,
    GfxShaders = false, -- По дефолту ВЫКЛЮЧЕНО
}

-- Таблица для хранения оригинальных настроек Lighting
local OriginalLighting = {
    FogColor = Lighting.FogColor,
    FogEnd = Lighting.FogEnd,
    FogStart = Lighting.FogStart,
    Ambient = Lighting.Ambient,
    OutdoorAmbient = Lighting.OutdoorAmbient,
    GlobalShadows = Lighting.GlobalShadows,
    ClockTime = Lighting.ClockTime,
}
local Connection = {}

local isActivationKeyPressed = false
local FOVCircle = Drawing.new("Circle")
FOVCircle.Visible = Settings.ShowFOV
FOVCircle.Radius = Settings.AimbotFOV
FOVCircle.Thickness = 1.5
FOVCircle.Color = Color3.fromRGB(0, 255, 204)
FOVCircle.Filled = false
FOVCircle.Position = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)

-- Функция применения/выключения шейдеров
local function ApplyGfx(state)
    if state then
        -- Неоновые бирюзовые шейдеры
        TweenService:Create(Lighting, TweenInfo.new(1), {
            FogColor = Color3.fromRGB(0, 30, 30),
            FogEnd = 500,
            FogStart = 50,
            Ambient = Color3.fromRGB(0, 100, 80),
            OutdoorAmbient = Color3.fromRGB(0, 20, 20),
            GlobalShadows = true,
            ClockTime = 0, -- Полночь
        }):Play()

        -- Замена Skybox
        local Sky = Instance.new("Sky")
        Sky.Name = "NeonWareSky"
        Sky.SkyboxBk = "rbxassetid://151165111"
        Sky.SkyboxDn = "rbxassetid://151165111"
        Sky.SkyboxFt = "rbxassetid://151165111"
        Sky.SkyboxLf = "rbxassetid://151165111"
        Sky.SkyboxRr = "rbxassetid://151165111"
        Sky.SkyboxUp = "rbxassetid://151165111"
        Sky.StarCount = 5000
        Sky.Parent = Lighting

    else
        -- Возврат к оригинальному освещению
        TweenService:Create(Lighting, TweenInfo.new(1), OriginalLighting):Play()
        local NeonSky = Lighting:FindFirstChild("NeonWareSky")
        if NeonSky then NeonSky:Destroy() end
    end
end

local function IsPlayerVisible(char)
    if not char or not char:FindFirstChild("HumanoidRootPart") then return false end
    local origin = Camera.CFrame.Position
    local targetPos = char.HumanoidRootPart.Position + Vector3.new(0, 1.6, 0)
    local dir = (targetPos - origin).Unit * (targetPos - origin).Magnitude
    local params = RaycastParams.new()
    params.FilterType = Enum.RaycastFilterType.Exclude
    params.FilterDescendantsInstances = {LocalPlayer.Character, char, Camera}
    local result = workspace:Raycast(origin, dir, params)
    if not result then return true end
    return result.Instance and result.Instance:IsDescendantOf(char)
end

local function GetClosestPlayer()
    local closest = nil
    local closestDist = Settings.AimbotFOV
    local mousePos = UserInputService:GetMouseLocation()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            local char = player.Character
            if IsPlayerVisible(char) then
                local realHeadPos = char.HumanoidRootPart.Position + Vector3.new(0, 1.6, 0)
                local pos, onScreen = Camera:WorldToViewportPoint(realHeadPos)
                if onScreen and pos.Z > 0 then
                    local dist = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                    if dist < closestDist then closestDist = dist; closest = player end
                end
            end
        end
    end
    return closest
end

local function InstantShoot()
    local x, y = Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2
    VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
    VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
end

local InputBeganCon = UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == Settings.AimbotKey then isActivationKeyPressed = true end
end)
table.insert(Connection, InputBeganCon)

local InputEndedCon = UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode == Settings.AimbotKey then isActivationKeyPressed = false end
end)
table.insert(Connection, InputEndedCon)

-- ===== СОЗДАНИЕ PREMIUM GUI =====
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SniperArenaUI_PremiumAnimated"
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 999999
ScreenGui.IgnoreGuiInset = true    
ScreenGui.Parent = TopGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 520, 0, 360)
MainFrame.Position = UDim2.new(0.5, -260, 0.5, -180)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 13)
MainFrame.Active = true
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 16)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 1.5
MainStroke.Color = Color3.fromRGB(0, 255, 204)
MainStroke.Transparency = 0.65
MainStroke.Parent = MainFrame

-- ПЛАВНЫЙ ДРАГГИНГ
local dragging, dragInput, dragStart, startPos
local function updateDrag(input)
    local delta = input.Position - dragStart
    TweenService:Create(MainFrame, TweenInfo.new(0.12, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    }):Play()
end

local DragInputBeganCon = MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true; dragStart = input.Position; startPos = MainFrame.Position
        input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
    end
end)
table.insert(Connection, DragInputBeganCon)

local DragInputChangedCon = MainFrame.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end end)
table.insert(Connection, DragInputChangedCon)

local DragInputChangedMasterCon = UserInputService.InputChanged:Connect(function(input) if input == dragInput and dragging then updateDrag(input) end end)
table.insert(Connection, DragInputChangedMasterCon)

local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 50)
TitleBar.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
TitleBar.BorderSizePixel = 0
TitleBar.ClipsDescendants = true
TitleBar.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 16)
TitleCorner.Parent = TitleBar

local TitleFix = Instance.new("Frame")
TitleFix.Size = UDim2.new(1, 0, 0, 20)
TitleFix.Position = UDim2.new(0, 0, 1, -20)
TitleFix.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
TitleFix.BorderSizePixel = 0
TitleFix.Parent = TitleBar

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 1, 0)
Title.BackgroundTransparency = 1
Title.Text = "   WIZZEY_WARE <font color='#00ffcc'>// neon arena</font>"
Title.RichText = true
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 15
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = TitleBar

local TabContainer = Instance.new("Frame")
TabContainer.Position = UDim2.new(0, 14, 0, 65)
TabContainer.Size = UDim2.new(0, 130, 1, -80)
TabContainer.BackgroundTransparency = 1
TabContainer.ClipsDescendants = true
TabContainer.Parent = MainFrame

local TabLayout = Instance.new("UIListLayout")
TabLayout.Padding = UDim.new(0, 8)
TabLayout.Parent = TabContainer

local ContentContainer = Instance.new("Frame")
ContentContainer.Position = UDim2.new(0, 160, 0, 65)
ContentContainer.Size = UDim2.new(1, -174, 1, -80)
ContentContainer.BackgroundColor3 = Color3.fromRGB(14, 14, 18)
ContentContainer.BorderSizePixel = 0
ContentContainer.ClipsDescendants = true
ContentContainer.Parent = MainFrame

local ContentCorner = Instance.new("UICorner")
ContentCorner.CornerRadius = UDim.new(0, 12)
ContentCorner.Parent = ContentContainer

local ContentStroke = Instance.new("UIStroke")
ContentStroke.Thickness = 1
ContentStroke.Color = Color3.fromRGB(30, 30, 35)
ContentStroke.Parent = ContentContainer

local Pages = {}
local Tabs = {
    {Name = "Аим", Icon = "🎯"}, {Name = "ВХ", Icon = "👁️"}, {Name = "Триггер", Icon = "⚡"}, {Name = "Gfx", Icon = "✨"}, {Name = "Misc", Icon = "⚙️"}
}
local currentTabName = "Аим"

local function CreatePage(name)
    local Page = Instance.new("CanvasGroup")
    Page.Size = UDim2.new(1, -20, 1, -20); Page.Position = UDim2.new(0, 10, 0, 10)
    Page.BackgroundTransparency = 1; Page.Visible = false; Page.GroupTransparency = 1; Page.ClipsDescendants = true
    Page.Parent = ContentContainer
    Instance.new("UIListLayout", Page).Padding = UDim.new(0, 10); Pages[name] = Page
    return Page
end

for _, tabData in ipairs(Tabs) do CreatePage(tabData.Name) end
Pages["Аим"].Visible = true; Pages["Аим"].GroupTransparency = 0

local currentActiveButton = nil
local function SwitchTab(tabName, button)
    if currentActiveButton == button then return end; currentTabName = tabName
    for name, page in pairs(Pages) do if page.Visible and name ~= tabName then TweenService:Create(page, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {GroupTransparency = 1}):Play(); task.delay(0.15, function() page.Visible = false end) end end
    local newPage = Pages[tabName]; newPage.Visible = true; TweenService:Create(newPage, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {GroupTransparency = 0}):Play()
    if currentActiveButton then TweenService:Create(currentActiveButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(16, 16, 22), TextColor3 = Color3.fromRGB(150, 150, 155)}):Play(); currentActiveButton:FindFirstChildOfClass("UIStroke").Color = Color3.fromRGB(30, 30, 35) end
    currentActiveButton = button; TweenService:Create(button, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(22, 35, 35), TextColor3 = Color3.fromRGB(0, 255, 204)}):Play(); button:FindFirstChildOfClass("UIStroke").Color = Color3.fromRGB(0, 255, 204)
end

local function CreateToggle(parent, text, default, callback)
    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.new(1, 0, 0, 42); Frame.BackgroundColor3 = Color3.fromRGB(18, 18, 24); Frame.ClipsDescendants = true; Frame.Parent = parent
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)
    local FStroke = Instance.new("UIStroke", Frame)
    FStroke.Thickness = 1; FStroke.Color = Color3.fromRGB(30, 30, 35)
    local Label = Instance.new("TextLabel", Frame)
    Label.Size = UDim2.new(0.7, 0, 1, 0); Label.Text = "    " .. text; Label.TextColor3 = Color3.fromRGB(230, 230, 235); Label.Font = Enum.Font.GothamMedium; Label.TextSize = 13; Label.TextXAlignment = Enum.TextXAlignment.Left; Label.BackgroundTransparency = 1
    local Button = Instance.new("TextButton", Frame)
    Button.Size = UDim2.new(0, 46, 0, 22); Button.Position = UDim2.new(1, -56, 0.5, -11); Button.BackgroundColor3 = default and Color3.fromRGB(0, 255, 204) or Color3.fromRGB(35, 35, 40); Button.Text = ""
    Instance.new("UICorner", Button).CornerRadius = UDim.new(1, 0)
    local CircleIndicator = Instance.new("Frame", Button)
    CircleIndicator.Size = UDim2.new(0, 16, 0, 16); CircleIndicator.Position = default and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8); CircleIndicator.BackgroundColor3 = default and Color3.fromRGB(10, 10, 15) or Color3.fromRGB(200, 200, 205)
    Instance.new("UICorner", CircleIndicator).CornerRadius = UDim.new(1, 0)
    local state = default
    Button.MouseButton1Click:Connect(function()
        state = not state; TweenService:Create(Button, TweenInfo.new(0.25, Enum.EasingStyle.Quad), {BackgroundColor3 = state and Color3.fromRGB(0, 255, 204) or Color3.fromRGB(35, 35, 40)}):Play(); TweenService:Create(CircleIndicator, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Position = state and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8), BackgroundColor3 = state and Color3.fromRGB(10, 10, 15) or Color3.fromRGB(200, 200, 205)}):Play(); callback(state)
    end)
end

local function CreateSlider(parent, text, min, max, default, callback)
    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.new(1, 0, 0, 56); Frame.BackgroundColor3 = Color3.fromRGB(18, 18, 24); Frame.ClipsDescendants = true; Frame.Parent = parent
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)
    local FStroke = Instance.new("UIStroke", Frame)
    FStroke.Thickness = 1; FStroke.Color = Color3.fromRGB(30, 30, 35)
    local Label = Instance.new("TextLabel", Frame)
    Label.Size = UDim2.new(1, 0, 0, 28); Label.Text = "    " .. text .. ": <font color='#00ffcc'>" .. tostring(default) .. "</font>"; Label.RichText = true; Label.TextColor3 = Color3.fromRGB(190, 190, 195); Label.Font = Enum.Font.GothamMedium; Label.TextSize = 13; Label.TextXAlignment = Enum.TextXAlignment.Left; Label.BackgroundTransparency = 1
    local SliderBG = Instance.new("TextButton", Frame)
    SliderBG.Size = UDim2.new(1, -32, 0, 6); SliderBG.Position = UDim2.new(0, 16, 0, 36); SliderBG.BackgroundColor3 = Color3.fromRGB(35, 35, 40); SliderBG.Text = ""; SliderBG.ClipsDescendants = true
    Instance.new("UICorner", SliderBG).CornerRadius = UDim.new(1, 0)
    local SliderFill = Instance.new("Frame", SliderBG)
    SliderFill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0); SliderFill.BackgroundColor3 = Color3.fromRGB(0, 255, 204); SliderFill.BorderSizePixel = 0
    Instance.new("UICorner", SliderFill).CornerRadius = UDim.new(1, 0)
    local IsDragging = false
    local function UpdateSlider()
        local MousePos = UserInputService:GetMouseLocation()
        local RelativeX = MousePos.X - SliderBG.AbsolutePosition.X
        local Percentage = math.clamp(RelativeX / SliderBG.AbsoluteSize.X, 0, 1)
        TweenService:Create(SliderFill, TweenInfo.new(0.08, Enum.EasingStyle.Quad), {Size = UDim2.new(Percentage, 0, 1, 0)}):Play()
        local Value = math.floor(min + (Percentage * (max - min)))
        Label.Text = "    " .. text .. ": <font color='#00ffcc'>" .. tostring(Value) .. "</font>"
        callback(Value)
    end
    SliderBG.MouseButton1Down:Connect(function() IsDragging = true; UpdateSlider() end)
    table.insert(Connection, UserInputService.InputChanged:Connect(function(input) if IsDragging and input.UserInputType == Enum.UserInputType.MouseMovement then UpdateSlider() end end))
    table.insert(Connection, UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then IsDragging = false end end))
end

local function CreateUnloadButton(parent, text, callback)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(1, 0, 0, 42); Button.BackgroundColor3 = Color3.fromRGB(45, 15, 20); Button.Text = text; Button.TextColor3 = Color3.fromRGB(255, 100, 100); Button.Font = Enum.Font.GothamBold; Button.TextSize = 13; Button.ClipsDescendants = true; Button.Parent = parent
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 8)
    local BStroke = Instance.new("UIStroke", Button)
    BStroke.Thickness = 1; BStroke.Color = Color3.fromRGB(120, 40, 50)
    Button.MouseEnter:Connect(function() TweenService:Create(Button, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(65, 20, 25)}):Play() end)
    Button.MouseLeave:Connect(function() TweenService:Create(Button, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(45, 15, 20)}):Play() end)
    Button.MouseButton1Click:Connect(callback)
end

-- Генерация элементов
for i, tabData in ipairs(Tabs) do
    local TBtn = Instance.new("TextButton", TabContainer)
    TBtn.Size = UDim2.new(1, 0, 0, 40); TBtn.BackgroundColor3 = (i == 1) and Color3.fromRGB(22, 35, 35) or Color3.fromRGB(16, 16, 22); TBtn.Text = "  " .. tabData.Icon .. "  " .. tabData.Name; TBtn.TextColor3 = (i == 1) and Color3.fromRGB(0, 255, 204) or Color3.fromRGB(150, 150, 155); TBtn.Font = Enum.Font.GothamBold; TBtn.TextSize = 13; TBtn.TextXAlignment = Enum.TextXAlignment.Left
    Instance.new("UICorner", TBtn).CornerRadius = UDim.new(0, 8)
    local BStroke = Instance.new("UIStroke", TBtn)
    BStroke.Thickness = 1; BStroke.Color = (i == 1) and Color3.fromRGB(0, 255, 204) or Color3.fromRGB(30, 30, 35)
    if i == 1 then currentActiveButton = TBtn end
    TBtn.MouseButton1Click:Connect(function() SwitchTab(tabData.Name, TBtn) end)
end

CreateToggle(Pages["Аим"], "Включить Аимбот", Settings.Aimbot, function(v) Settings.Aimbot = v end)
CreateToggle(Pages["Аим"], "Показывать Круг FOV", Settings.ShowFOV, function(v) Settings.ShowFOV = v end) 
CreateSlider(Pages["Аим"], "Радиус FOV", 10, 600, Settings.AimbotFOV, function(v) Settings.AimbotFOV = v; FOVCircle.Radius = v end)
CreateToggle(Pages["ВХ"], "Включить ESP (Боксы)", Settings.ESP_Box, function(v) Settings.ESP_Box = v end)
CreateToggle(Pages["Триггер"], "Включить Триггербот", Settings.TriggerBot, function(v) Settings.TriggerBot = v end)

-- ВКЛАДКА GFX
CreateToggle(Pages["Gfx"], "Включить неоновые шейдеры", Settings.GfxShaders, function(v) Settings.GfxShaders = v; ApplyGfx(v) end)

-- ВКЛАДКА MISC
CreateToggle(Pages["Misc"], "Включить расширение голов", Settings.ExpandHeads, function(v) Settings.ExpandHeads = v end)
CreateSlider(Pages["Misc"], "Размер хитбокса головы", 1, 15, Settings.HeadSize, function(v) Settings.HeadSize = v end)

local function UnloadScript()
    ScriptActive = false
    ApplyGfx(false) -- Выключаем шейдеры перед выгрузкой
    for _, connection in pairs(Connection) do if connection then connection:Disconnect() end end
    if FOVCircle then FOVCircle.Visible = false; FOVCircle:Remove() end
    for _, player in pairs(Players:GetPlayers()) do if player.Character then local head = player.Character:FindFirstChild("Head") if head and head:IsA("BasePart") then head.Size = Vector3.new(1.2, 1.2, 1.2); head.Transparency = 0 end; local esp = player.Character:FindFirstChild("PoloESP") if esp then esp:Destroy() end end end
    if ScreenGui then ScreenGui:Destroy() end
end
CreateUnloadButton(Pages["Misc"], "ВЫГРУЗИТЬ СОФТ", UnloadScript)

local menuVisible = true
local
