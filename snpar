local a = "Sniper Arena Script by wizzey"
local b = string.char
local c = string.gsub
local d = {}
local e = 0

local f = {
    {"game:GetService", 0},
    {"Players", 1},
    {"RunService", 2},
    {"UserInputService", 3},
    {"workspace.CurrentCamera", 4},
    {"LocalPlayer", 5},
    {"GetMouse", 6},
    {"FindFirstChild", 7},
    {"Position", 8},
    {"CFrame", 9},
    {"RaycastParams", 10},
    {"Raycast", 11},
    {"WorldToViewportPoint", 12},
    {"GetMouseLocation", 13},
    {"Insert", 14},
    {"Remove", 15},
    {"new", 16},
    {"Circle", 17},
    {"Line", 18},
    {"Text", 19},
    {"Highlight", 20},
    {"AlwaysOnTop", 21},
    {"Connect", 22},
    {"RenderStepped", 23},
    {"InputBegan", 24},
    {"InputEnded", 25},
    {"KeyCode", 26},
    {"Wait", 27},
    {"mouse1click", 28},
    {"mousemoverel", 29},
    {"setclipboard", 30},
    {"HttpGet", 31},
    {"loadstring", 32},
    {"warn", 33},
    {"print", 34},
    {"task.wait", 35},
    {"tick", 36}
}

local function g(h)
    for i, j in pairs(f) do
        if j[1] == h then
            return "A" .. j[2] .. "B"
        end
    end
    return h
end

local k = [[
local l=game:GetService("Players")
local m=game:GetService("RunService")
local n=game:GetService("UserInputService")
local o=workspace.CurrentCamera
local p=l.LocalPlayer
local q=p:GetMouse()
local r={aim=true,fov=300,key=Enum.KeyCode.E,smooth=1,wall=true,trig=true,prec=15,del=0.05,esp=true}
local s=false
local t={}
local u=true
local function v(w)
local x=w:FindFirstChild("Head")
return x and x.Position or nil
end
local function y(z)
if not z or not z:FindFirstChild("Head") then return false end
local A=z.Head
local B=o.CFrame.Position
local C=(A.Position-B).Unit*(A.Position-B).Magnitude
local D=RaycastParams.new()
D.FilterType=Enum.RaycastFilterType.Blacklist
D.FilterDescendantsInstances={p.Character,z}
local E=workspace:Raycast(B,C,D)
if not E then return true end
return E.Instance and E.Instance:IsDescendantOf(z)
end
local function F()
local G=nil
local H=r.fov
local I=n:GetMouseLocation()
for _,J in pairs(l:GetPlayers())do
if J~=p and J.Character and J.Character:FindFirstChild("Head")then
local K=J.Character
if r.wall and not y(K)then continue end
local L=v(K)
if L then
local M,N=o:WorldToViewportPoint(L)
if N and M.Z>0 then
local O=(Vector2.new(M.X,M.Y)-I).Magnitude
if O<H then
H=O
G=J
end
end
end
end
end
return G
end
local function P(Q)
if not Q or not Q.Character then return false end
local R=Q.Character:FindFirstChild("Head")
if not R then return false end
if r.wall and not y(Q.Character)then return false end
local S,T=o:WorldToViewportPoint(R.Position)
if not T then return false end
local U=n:GetMouseLocation()
local V=(Vector2.new(S.X,S.Y)-U).Magnitude
return V<r.prec
end
local function W()
if not u then return end
u=false
pcall(function()mouse1click()end)
task.wait(r.del)
u=true
end
m.RenderStepped:Connect(function()
for _,X in pairs(t)do pcall(function()X:Remove()end)end
t={}
local Y=Drawing.new("Circle")
Y.Visible=true
Y.Radius=r.fov
Y.Thickness=1
Y.Color=Color3.fromRGB(255,255,255)
Y.Filled=false
Y.Position=Vector2.new(o.ViewportSize.X/2,o.ViewportSize.Y/2)
table.insert(t,Y)
for _,Z in pairs(l:GetPlayers())do
if Z~=p and Z.Character and Z.Character:FindFirstChild("Head")then
local aa=Z.Character
local ab=y(aa)
if r.esp and ab then
local ac=aa:FindFirstChild("PoloESP")
if not ac then
ac=Instance.new("Highlight")
ac.Name="PoloESP"
ac.Parent=aa
end
ac.FillColor=Color3.fromRGB(255,0,0)
ac.OutlineColor=Color3.fromRGB(255,255,255)
ac.FillTransparency=0.4
ac.DepthMode=Enum.HighlightDepthMode.AlwaysOnTop
else
local ad=aa:FindFirstChild("PoloESP")
if ad then ad:Destroy()end
end
end
end
if r.trig and u then
local ae=F()
if ae and P(ae)then W()end
end
if r.aim and s then
local af=F()
if af then
local ag=v(af.Character)
if ag then
local ah=o:WorldToViewportPoint(ag)
local ai=n:GetMouseLocation()
local aj=ah.X-ai.X
local ak=ah.Y-ai.Y
if math.abs(aj)>2 or math.abs(ak)>2 then
pcall(function()
if r.smooth<=1 then
mousemoverel(aj,ak)
else
mousemoverel(aj/r.smooth,ak/r.smooth)
end
end)
end
end
end
end
end)
n.InputBegan:Connect(function(al)
if al.KeyCode==r.key then s=true end
end)
n.InputEnded:Connect(function(am)
if am.KeyCode==r.key then s=false end
end)
pcall(function()
game:GetService("StarterGui"):SetCore("SendNotification",{
Title="Sniper Arena",
Text="E - Aimbot | WallCheck ON",
Duration=3
})
end)
]]

-- Кодируем в Base64
local an = game:GetService("HttpService"):Base64Encode(k)

-- Финальный загрузчик
local ao = [[
local ap=game:GetService("HttpService"):Base64Decode("]]..an..[[")

local function aq(ar)
    local as = {}
    local at, au = string.gmatch(ar, "([^%s]+)") 
end

local av = loadstring(ap)
if av then av() else print("Error loading script") end
]]

print(ao)
