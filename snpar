-- Polo Sniper Arena - ULTRA FAST (Мгновенный аим + Trigger)
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

-- ===== ADMIN LIST =====
local AdminIDs = {
    -- СЮДА ВСТАВИТЬ ВАШИ АЙДИ АДМИНОВ
    -- Пример: 123456789,
}

-- ===== НАСТРОЙКИ =====
local Settings = {
    Aimbot = true,
    AimbotFOV = 350,
    AimbotKey = Enum.KeyCode.E,
    AimbotWallCheck = true,
    
    TriggerBot = true,
    TriggerDelay = 0.01,
    TriggerPrecision = 15,
    
    ESP_Box = true,
    ESP_Visible = true,
    WallCheck = true,
    
    NoRecoil = true,
    NoSpread = true,
    FastReload = true,
    
    AdminCheck = true,
    AdminLeave = true,
}

-- ===== ПЕРЕМЕННЫЕ =====
local isAimbotKeyPressed = false
local Drawings = {}
local canShoot = true
local currentTarget = nil
local isGUIOpen = true

-- ===== ПРОВЕРКА АДМИНОВ =====
local function CheckAdmins()
    if not Settings.AdminCheck then return end
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local userId = player.UserId
            for _, adminId in pairs(AdminIDs) do
                if userId == adminId then
                    if Settings.AdminLeave then
                        pcall(function()
                            game:GetService("StarterGui"):SetCore("SendNotification", {
                                Title = "⚠️ ADMIN DETECTED",
                                Text = player.Name .. " is an admin! Leaving server...",
                                Duration = 5
                            })
                        end)
                        wait(1)
                        game:GetService("TeleportService"):Teleport(game.PlaceId)
                        return
                    end
                end
            end
        end
    end
end

-- ===== ФУНКЦИИ =====
local function GetHeadPosition(character)
    local head = character:FindFirstChild("Head")
    if head then
        return head.Position
    end
    return nil
end

local function IsPlayerVisible(character)
    if not character or not character:FindFirstChild("Head") then
        return false
    end
    
    local head = character.Head
    local origin = Camera.CFrame.Position
    local direction = (head.Position - origin).Unit * (head.Position - origin).Magnitude
    
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
    raycastParams.FilterDescendantsInstances = {LocalPlayer.Character, character}
    
    local raycastResult = workspace:Raycast(origin, direction, raycastParams)
    
    if not raycastResult then
        return true
    end
    
    local hitPart = raycastResult.Instance
    if hitPart and hitPart:IsDescendantOf(character) then
        return true
    end
    
    return false
end

-- ===== МГНОВЕННЫЙ ТРИГГЕРБОТ =====
local function IsAimingAtHead(target)
    if not target or not target.Character then return false end
    
    local head = target.Character:FindFirstChild("Head")
    if not head then return false end
    
    local headPos = head.Position
    local screenPos, onScreen = Camera:WorldToViewportPoint(headPos)
    
    if not onScreen then return false end
    
    local mousePos = UserInputService:GetMouseLocation()
    local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
    
    if dist > Settings.TriggerPrecision then
        return false
    end
    
    local humanoid = target.Character:FindFirstChild("Humanoid")
    if not humanoid or humanoid.Health <= 0 then
        return false
    end
    
    return true
end

local function GetTriggerTarget()
    local bestTarget = nil
    local bestDist = math.huge
    local mousePos = UserInputService:GetMouseLocation()
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local character = player.Character
            
            if not IsPlayerVisible(character) then
                continue
            end
            
            local headPos = GetHeadPosition(character)
            if headPos then
                local pos, onScreen = Camera:WorldToViewportPoint(headPos)
                
                if onScreen and pos.Z > 0 then
                    local dist = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                    if dist < bestDist and dist < Settings.AimbotFOV then
                        bestDist = dist
                        bestTarget = player
                    end
                end
            end
        end
    end
    
    return bestTarget
end

-- ===== МГНОВЕННЫЙ АИМ (БЕЗ СГЛАЖИВАНИЯ) =====
local function GetClosestPlayer()
    local closest = nil
    local closestDist = math.huge
    local mousePos = UserInputService:GetMouseLocation()
    
    if currentTarget and currentTarget.Character and currentTarget.Character:FindFirstChild("Head") then
        local headPos = GetHeadPosition(currentTarget.Character)
        if headPos then
            local pos, onScreen = Camera:WorldToViewportPoint(headPos)
            if onScreen and pos.Z > 0 then
                local dist = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                if dist < Settings.AimbotFOV then
                    if Settings.AimbotWallCheck then
                        if IsPlayerVisible(currentTarget.Character) then
                            return currentTarget
                        else
                            currentTarget = nil
                        end
                    else
                        return currentTarget
                    end
                end
            end
        end
    end
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local character = player.Character
            
            if Settings.AimbotWallCheck then
                if not IsPlayerVisible(character) then
                    continue
                end
            end
            
            local headPos = GetHeadPosition(character)
            if headPos then
                local pos, onScreen = Camera:WorldToViewportPoint(headPos)
                
                if onScreen and pos.Z > 0 then
                    local dist = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                    if dist < closestDist and dist < Settings.AimbotFOV then
                        closestDist = dist
                        closest = player
                    end
                end
            end
        end
    end
    
    if closest then
        currentTarget = closest
    end
    
    return closest
end

-- ===== МГНОВЕННОЕ НАВЕДЕНИЕ (ТЕЛЕПОРТ) =====
local function InstantAim(target)
    if not target or not target.Character then return end
    
    local character = target.Character
    
    if Settings.AimbotWallCheck then
        if not IsPlayerVisible(character) then
            return
        end
    end
    
    local headPos = GetHeadPosition(character)
    if not headPos then return end
    
    -- Мгновенный поворот камеры
    local camPos = Camera.CFrame.Position
    local newCF = CFrame.lookAt(camPos, headPos)
    Camera.CFrame = newCF
    
    -- Мгновенное движение мыши
    local screenPos, onScreen = Camera:WorldToViewportPoint(headPos)
    if onScreen then
        local mousePos = UserInputService:GetMouseLocation()
        local deltaX = screenPos.X - mousePos.X
        local deltaY = screenPos.Y - mousePos.Y
        
        if math.abs(deltaX) > 0.5 or math.abs(deltaY) > 0.5 then
            pcall(function()
                mousemoverel(deltaX, deltaY)
            end)
        end
    end
end

-- ===== МГНОВЕННЫЙ ВЫСТРЕЛ =====
local function InstantShoot()
    if not canShoot then return end
    canShoot = false
    
    pcall(function()
        mouse1click()
    end)
    
    task.wait(Settings.TriggerDelay)
    canShoot = true
end

local function ApplyWeaponMods()
    pcall(function()
        local tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
        if tool then
            for _, child in pairs(tool:GetDescendants()) do
                if child:IsA("NumberValue") or child:IsA("DoubleConstrainedValue") or child:IsA("IntValue") then
                    local name = child.Name:lower()
                    
                    if Settings.NoRecoil and (name:find("recoil") or name:find("kick")) then
                        child.Value = 0
                    end
                    
                    if Settings.NoSpread and (name:find("spread") or name:find("accuracy")) then
                        child.Value = 0
                    end
                    
                    if Settings.FastReload and name:find("reload") then
                        child.Value = 0.01
                    end
                end
            end
        end
    end)
end

local function DrawFOV()
    local fovCircle = Drawing.new("Circle")
    fovCircle.Visible = true
    fovCircle.Radius = Settings.AimbotFOV
    fovCircle.Thickness = 2
    fovCircle.Color = Color3.fromRGB(255, 255, 255)
    fovCircle.Filled = false
    fovCircle.Position = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
    table.insert(Drawings, fovCircle)
    
    local dot = Drawing.new("Circle")
    dot.Visible = true
    dot.Radius = 2
    dot.Color = Color3.fromRGB(255, 0, 0)
    dot.Filled = true
    dot.Position = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
    table.insert(Drawings, dot)
end

-- ===== GUI =====
local function CreateGUI()
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "PoloGUI"
    screenGui.Parent = game:GetService("CoreGui")
    
    local mainFrame = Instance.new("Frame")
    mainFrame.Name = "MainFrame"
    mainFrame.Size = UDim2.new(0, 350, 0, 500)
    mainFrame.Position = UDim2.new(0.5, -175, 0.5, -250)
    mainFrame.BackgroundColor3 = Color3.fromRGB(30, 15, 50)
    mainFrame.BackgroundTransparency = 0.95
    mainFrame.BorderSizePixel = 0
    mainFrame.Parent = screenGui
    
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 40)
    title.Position = UDim2.new(0, 0, 0, 0)
    title.BackgroundColor3 = Color3.fromRGB(100, 40, 180)
    title.BackgroundTransparency = 0.3
    title.BorderSizePixel = 0
    title.Text = "✨ Polo Sniper Arena ✨"
    title.TextColor3 = Color3.fromRGB(220, 180, 255)
    title.TextScaled = true
    title.Font = Enum.Font.GothamBold
    title.Parent = mainFrame
    
    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 30, 0, 30)
    closeBtn.Position = UDim2.new(1, -35, 0, 5)
    closeBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    closeBtn.BackgroundTransparency = 0.5
    closeBtn.BorderSizePixel = 0
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeBtn.TextSize = 18
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.Parent = mainFrame
    
    local scrollFrame = Instance.new("ScrollingFrame")
    scrollFrame.Size = UDim2.new(1, -10, 1, -50)
    scrollFrame.Position = UDim2.new(0, 5, 0, 45)
    scrollFrame.BackgroundTransparency = 1
    scrollFrame.BorderSizePixel = 0
    scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
    scrollFrame.ScrollBarThickness = 4
    scrollFrame.Parent = mainFrame
    
    local listLayout = Instance.new("UIListLayout")
    listLayout.Padding = UDim.new(0, 8)
    listLayout.Parent = scrollFrame
    
    local function CreateToggle(text, setting, default)
        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(1, 0, 0, 35)
        frame.BackgroundColor3 = Color3.fromRGB(60, 30, 100)
        frame.BackgroundTransparency = 0.5
        frame.BorderSizePixel = 0
        frame.Parent = scrollFrame
        
        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(0.7, -10, 1, 0)
        label.Position = UDim2.new(0, 10, 0, 0)
        label.BackgroundTransparency = 1
        label.Text = text
        label.TextColor3 = Color3.fromRGB(220, 200, 255)
        label.TextSize = 14
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Font = Enum.Font.Gotham
        label.Parent = frame
        
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0, 40, 0, 25)
        btn.Position = UDim2.new(1, -50, 0.5, -12.5)
        btn.BackgroundColor3 = default and Color3.fromRGB(100, 50, 200) or Color3.fromRGB(60, 30, 100)
        btn.BorderSizePixel = 0
        btn.Text = default and "ON" or "OFF"
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.TextSize = 12
        btn.Font = Enum.Font.GothamBold
        btn.Parent = frame
        
        local state = default
        btn.MouseButton1Click:Connect(function()
            state = not state
            Settings[setting] = state
            btn.BackgroundColor3 = state and Color3.fromRGB(100, 50, 200) or Color3.fromRGB(60, 30, 100)
            btn.Text = state and "ON" or "OFF"
        end)
        
        return btn
    end
    
    local function CreateSlider(text, setting, min, max, default)
        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(1, 0, 0, 45)
        frame.BackgroundColor3 = Color3.fromRGB(60, 30, 100)
        frame.BackgroundTransparency = 0.5
        frame.BorderSizePixel = 0
        frame.Parent = scrollFrame
        
        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, -20, 0, 20)
        label.Position = UDim2.new(0, 10, 0, 0)
        label.BackgroundTransparency = 1
        label.Text = text .. ": " .. tostring(default)
        label.TextColor3 = Color3.fromRGB(220, 200, 255)
        label.TextSize = 13
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Font = Enum.Font.Gotham
        label.Parent = frame
        
        local slider = Instance.new("Frame")
        slider.Size = UDim2.new(0.9, 0, 0, 4)
        slider.Position = UDim2.new(0.05, 0, 0, 28)
        slider.BackgroundColor3 = Color3.fromRGB(40, 20, 70)
        slider.BorderSizePixel = 0
        slider.Parent = frame
        
        local fill = Instance.new("Frame")
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = Color3.fromRGB(150, 80, 255)
        fill.BorderSizePixel = 0
        fill.Parent = slider
        
        local dragging = false
        local value = default
        
        local function updateSlider(input)
            local pos = math.clamp((input.Position.X - slider.AbsolutePosition.X) / slider.AbsoluteSize.X, 0, 1)
            value = min + pos * (max - min)
            value = math.round(value)
            Settings[setting] = value
            fill.Size = UDim2.new(pos, 0, 1, 0)
            label.Text = text .. ": " .. tostring(value)
        end
        
        slider.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                dragging = true
                updateSlider(input)
            end
        end)
        
        slider.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                dragging = false
            end
        end)
        
        slider.InputChanged:Connect(function(input)
            if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
                updateSlider(input)
            end
        end)
        
        return slider
    end
    
    CreateToggle("🎯 Aimbot", "Aimbot", true)
    CreateSlider("🎯 Aimbot FOV", "AimbotFOV", 50, 500, 350)
    CreateToggle("🧱 Wall Check", "AimbotWallCheck", true)
    
    CreateToggle("🔫 TriggerBot", "TriggerBot", true)
    CreateSlider("🎯 Trigger Precision", "TriggerPrecision", 5, 30, 15)
    
    CreateToggle("👁️ ESP Box", "ESP_Box", true)
    CreateToggle("👁️ ESP Through Walls", "ESP_Visible", true)
    
    CreateToggle("🔧 No Recoil", "NoRecoil", true)
    CreateToggle("🔧 No Spread", "NoSpread", true)
    CreateToggle("🔧 Fast Reload", "FastReload", true)
    
    CreateToggle("🛡️ Admin Check", "AdminCheck", true)
    CreateToggle("🚪 Auto Leave on Admin", "AdminLeave", true)
    
    local guiOpen = true
    closeBtn.MouseButton1Click:Connect(function()
        guiOpen = not guiOpen
        mainFrame.Visible = guiOpen
        isGUIOpen = guiOpen
    end)
    
    UserInputService.InputBegan:Connect(function(input)
        if input.KeyCode == Enum.KeyCode.RightShift then
            guiOpen = not guiOpen
            mainFrame.Visible = guiOpen
            isGUIOpen = guiOpen
        end
    end)
    
    return screenGui
end

CreateGUI()

-- ===== ПРОВЕРКА АДМИНОВ =====
spawn(function()
    while true do
        wait(3)
        CheckAdmins()
    end
end)

Players.PlayerAdded:Connect(function(player)
    wait(1)
    CheckAdmins()
end)

-- ===== КЛАВИШИ =====
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Settings.AimbotKey then
        isAimbotKeyPressed = true
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode == Settings.AimbotKey then
        isAimbotKeyPressed = false
        currentTarget = nil
    end
end)

-- ===== ОСНОВНОЙ ЦИКЛ =====
RunService.RenderStepped:Connect(function()
    for _, drawing in pairs(Drawings) do
        pcall(function() drawing:Remove() end)
    end
    Drawings = {}
    
    DrawFOV()
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local character = player.Character
            local isVisible = IsPlayerVisible(character)
            
            if not Settings.ESP_Visible and not isVisible then
                local highlight = character:FindFirstChild("PoloESP")
                if highlight then highlight:Destroy() end
                continue
            end
            
            if Settings.ESP_Box then
                local highlight = character:FindFirstChild("PoloESP")
                if not highlight then
                    highlight = Instance.new("Highlight", character)
                    highlight.Name = "PoloESP"
                    highlight.FillColor = isVisible and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(255, 255, 0)
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                    highlight.FillTransparency = 0.3
                    if Settings.ESP_Visible then
                        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                    end
                end
            end
        end
    end
    
    -- ===== МГНОВЕННЫЙ ТРИГГЕРБОТ =====
    if Settings.TriggerBot and canShoot then
        local target = GetTriggerTarget()
        if target then
            if IsAimingAtHead(target) then
                local humanoid = target.Character:FindFirstChild("Humanoid")
                if humanoid and humanoid.Health > 0 then
                    InstantShoot()
                end
            end
        end
    end
    
    -- ===== МГНОВЕННЫЙ АИМ =====
    if Settings.Aimbot and isAimbotKeyPressed then
        local target = GetClosestPlayer()
        if target then
            InstantAim(target)
        end
    end
    
    if Settings.NoRecoil or Settings.NoSpread or Settings.FastReload then
        ApplyWeaponMods()
    end
end)

pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "Polo Sniper Arena",
        Text = "Press Right Shift for GUI | Hold E for INSTANT aimbot",
        Duration = 3
    })
end)

print("=== Polo Sniper Arena ULTRA FAST loaded ===")
print("Press Right Shift to toggle GUI")
print("Hold E for INSTANT aimbot (телепорт)")
print("TriggerBot: МГНОВЕННЫЙ")
