-- Polo Sniper Arena - FULL (ESP through walls + Perfect Aimbot + TriggerBot)
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

local Settings = {
    Aimbot = true,
    AimbotSmooth = 0.02,
    AimbotFOV = 300,
    TriggerBot = true,      -- ВКЛЮЧЕН ТРИГГЕРБОТ
    TriggerDelay = 0.05,    -- Задержка перед выстрелом (сек)
    ESP_Box = true,
    VisCheck = false
}

local isKeyPressed = false
local Drawings = {}
local canShoot = true
local lastShotTime = 0

-- Клавиша E (Аимбот)
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.E then
        isKeyPressed = true
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.E then
        isKeyPressed = false
    end
end)

-- Функция для клика мышкой (выстрел)
local function Shoot()
    if not canShoot then return end
    canShoot = false
    
    pcall(function()
        mouse1click() -- Нажатие ЛКМ
    end)
    
    -- Задержка перед следующим выстрелом
    task.wait(Settings.TriggerDelay)
    canShoot = true
end

-- Получение позиции головы
local function GetHeadPosition(character)
    local head = character:FindFirstChild("Head")
    if head then
        return head.Position
    end
    return nil
end

-- Проверка, наведен ли прицел на голову (с погрешностью)
local function IsAimingAtHead(target)
    if not target or not target.Character then return false end
    
    local headPos = GetHeadPosition(target.Character)
    if not headPos then return false end
    
    local screenPos = Camera:WorldToViewportPoint(headPos)
    local mousePos = UserInputService:GetMouseLocation()
    
    -- Расстояние от прицела до головы (в пикселях)
    local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
    
    -- Точность наведения (чем меньше, тем точнее)
    local AIM_PRECISION = 15 -- Пикселей (можно настроить)
    
    return dist < AIM_PRECISION
end

-- Получение ближайшего игрока
local function GetClosestPlayer()
    local closest = nil
    local closestDist = math.huge
    local mousePos = UserInputService:GetMouseLocation()
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local character = player.Character
            local headPos = GetHeadPosition(character)
            
            if headPos then
                local pos, onScreen = Camera:WorldToViewportPoint(headPos)
                
                if pos.Z > 0 then
                    local dist = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                    if dist < closestDist and dist < Settings.AimbotFOV then
                        closestDist = dist
                        closest = player
                    end
                end
            end
        end
    end
    return closest
end

-- Получение лучшей цели (для триггербота)
local function GetTargetForTrigger()
    local bestTarget = nil
    local bestDist = math.huge
    local mousePos = UserInputService:GetMouseLocation()
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local character = player.Character
            local headPos = GetHeadPosition(character)
            
            if headPos then
                local pos, onScreen = Camera:WorldToViewportPoint(headPos)
                
                if pos.Z > 0 then
                    local dist = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                    if dist < bestDist and dist < Settings.AimbotFOV then
                        bestDist = dist
                        bestTarget = player
                    end
                end
            end
        end
    end
    
    return bestTarget, bestDist
end

-- Главный цикл
RunService.RenderStepped:Connect(function()
    -- Очистка старых рисунков
    for _, drawing in pairs(Drawings) do
        pcall(function() drawing:Remove() end)
    end
    Drawings = {}
    
    -- FOV круг
    local fovCircle = Drawing.new("Circle")
    fovCircle.Visible = true
    fovCircle.Radius = Settings.AimbotFOV
    fovCircle.Thickness = 2
    fovCircle.Color = Color3.fromRGB(255, 255, 255)
    fovCircle.Filled = false
    fovCircle.Position = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
    table.insert(Drawings, fovCircle)
    
    -- ESP Box (через стены)
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local character = player.Character
            
            if Settings.ESP_Box then
                local highlight = character:FindFirstChild("PoloESP")
                if not highlight then
                    highlight = Instance.new("Highlight", character)
                    highlight.Name = "PoloESP"
                    highlight.FillColor = Color3.fromRGB(255, 0, 0)
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                    highlight.FillTransparency = 0.3
                    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                end
            end
        end
    end
    
    -- ===== TRIGGERBOT =====
    if Settings.TriggerBot and canShoot then
        local target, dist = GetTargetForTrigger()
        if target and dist then
            -- Проверяем, точно ли наведен прицел на голову
            if IsAimingAtHead(target) then
                -- Проверяем, жив ли игрок
                local humanoid = target.Character:FindFirstChild("Humanoid")
                if humanoid and humanoid.Health > 0 then
                    Shoot()
                end
            end
        end
    end
    
    -- ===== AIMBOT =====
    if Settings.Aimbot and isKeyPressed then
        local target = GetClosestPlayer()
        if target then
            local character = target.Character
            if character then
                local headPos = GetHeadPosition(character)
                if headPos then
                    local screenPos = Camera:WorldToViewportPoint(headPos)
                    local mousePos = UserInputService:GetMouseLocation()
                    
                    local deltaX = screenPos.X - mousePos.X
                    local deltaY = screenPos.Y - mousePos.Y
                    
                    if math.abs(deltaX) > 3 or math.abs(deltaY) > 3 then
                        pcall(function()
                            mousemoverel(deltaX * 0.9, deltaY * 0.9)
                        end)
                    else
                        pcall(function()
                            mousemoverel(deltaX, deltaY)
                        end)
                    end
                end
            end
        end
    end
end)

-- Уведомление
pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "Polo Sniper Arena FULL",
        Text = "ESP + Aimbot + TriggerBot | Hold E for aimbot",
        Duration = 3
    })
end)

print("Polo Sniper Arena FULL loaded!")
print("ESP through walls | Hold E for aimbot | Auto-shoot on head")
